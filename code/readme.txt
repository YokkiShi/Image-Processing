1. All images should own the same name as downloaded from the QMPLUS img20142. The file write in the script file is to be executed in the command window together with the related functions which locates in the function folder.3. An example exe1_a_ppm   %type following instructions in the command window:%showing binary files Baboon512C_Binary=ppmread('/Baboon512C_Binary.ppm');imshow(Baboon512C_Binary);(it presume the file Baboon512C_Binary.ppm lay in the same folder as the function ppmread )